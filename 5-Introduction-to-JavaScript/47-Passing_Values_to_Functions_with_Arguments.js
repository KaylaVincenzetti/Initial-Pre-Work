//learned about passing values into functions. Parameters are placeholder variables. Arguments are values passed into function when called

// Example
function ourFunctionWithArgs(a, b) {
  console.log(a - b);
}
ourFunctionWithArgs(10, 5); // Outputs 5

// Only change code below this line.
//created function with two parameters num1 and num2. Passed in arguments fo 23 and 9 when function was called
function functionWithArgs(num1, num2) {
  console.log(num1 + num2);
}

functionWithArgs(23, 9);
