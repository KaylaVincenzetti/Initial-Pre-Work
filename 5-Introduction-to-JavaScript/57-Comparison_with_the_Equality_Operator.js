//learned about equality operators, thay compare two values and will return true if they are equal, otherwise returns false


// Setup
function testEqual(val) {
  //changed this line of code to check if val is equal to 12
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
