//learned about the parseInt function, it turns a string into an integer

function convertToInteger(str) {
  //converted input of str to interger using parseInt
  return parseInt(str);

}

convertToInteger("56");
