//learned how to use the logicial or operator, this will return true if either opperand is true

function testLogicalOr(val) {
  // Only change code below this line
//updated code to use logicial Or operator to check if val is greater than 20 or less than 10
  if (val > 20 || val < 10) {
    return "Outside";
  }


  // Only change code above this line
  return "Inside";
}

// Change this value to test
testLogicalOr(15);
