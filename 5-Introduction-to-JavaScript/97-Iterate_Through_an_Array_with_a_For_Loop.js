//learned how to iterate through an array with a for loop

// Example
var ourArr = [ 9, 10, 11, 12];
var ourTotal = 0;

for (var i = 0; i < ourArr.length; i++) {
  ourTotal += ourArr[i];
}

// Setup
var myArr = [ 2, 3, 4, 5, 6];
//declared and initalized total variable
var total = 0;
//created a for loop to iterate through myArr
for(var i = 0; i < myArr.length; i++) {
  total += myArr[i];
}

// Only change code below this line
