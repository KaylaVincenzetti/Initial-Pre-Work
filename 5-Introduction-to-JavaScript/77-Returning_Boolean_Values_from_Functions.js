//learned we don't need to use if statments to do comparisons if the value will return true/false
//removed if statments and set up to return true of false with use of comparison operator
function isLess(a, b) {
  // Fix this code

    return a < b;

}

// Change these values to test
isLess(10, 15);
