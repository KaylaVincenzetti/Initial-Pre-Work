//used multiple conditional operators to check if number is positive, negative or zero

function checkSign(num) {
   return (num > 0) ? "positive" : (num < 0) ? "negative" : "zero";
}

checkSign(10);
