//learned about the inequality operator and how it will also convert data types like the equality operator

// Setup
function testNotEqual(val) {
  //set to return not equal if val doesn;t equal 99 with inequality operator !=
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
