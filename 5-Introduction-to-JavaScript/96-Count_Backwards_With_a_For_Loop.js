//learned we can also set up a for loop to iterate backwards through numbers

// Example
var ourArray = [];

for (var i = 10; i > 0; i -= 2) {
  ourArray.push(i);
}

// Setup
//set up a for loop to count backwards from 1-9 while decrementing by 2 and push odd numbers to myArray
var myArray = [];
for(i = 9; i > 0; i -=2) {
  myArray.push(i);
}

// Only change code below this line.
