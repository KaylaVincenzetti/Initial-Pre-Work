//learned about the Math.random function

function randomFraction() {

  // Only change code below this line.
    var result = 0;
    //created a while loop and used Math.random to return a random number instead of 0
    while(result === 0) {
        result = Math.random();
    }
  return result;

  // Only change code above this line.
}
