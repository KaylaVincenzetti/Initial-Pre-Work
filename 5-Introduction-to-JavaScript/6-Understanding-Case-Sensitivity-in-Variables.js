/* learned it's possible to have multiple variables with the same name but differnet casing. This in not recommended
the preferred method is to use camelCase where the first word is lower case and each word after starts with an upper case letter */

//changed the casing on the declerations and assignments to camelCase

// Declarations
var studlyCapVar;
var properCamelCase;
var titleCaseOver;

// Assignments
studlyCapVar = 10;
properCamelCase = "A String";
titleCaseOver = 9000;
